fastapi==0.111.0
uvicorn==0.30.1
requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.2.2
httpx==0.27.0
python-dotenv==1.0.1
apscheduler==3.10.4
pywebpush==2.0.1
py-vapid==1.9.0
pydantic==2.7.1
pdfplumber==0.11.4
python-multipart==0.0.9
# NOTE: the newest cloudscraper the package index offers is 1.2.71 — no
# 2.x is available. A "cloudscraper==2.1.0.post1" pin lived here briefly
# (commit 5300093) and broke `pip install -r requirements.txt` with
# "No matching distribution found". Only pin a version that actually
# installs.
cloudscraper==1.2.71
